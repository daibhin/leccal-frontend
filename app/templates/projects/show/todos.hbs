<ul class="list list-normal flex with_columns">
  {{#each model.todos as |todo|}}
    <div class="list-normal-item block flex with_rows">
      <span>{{todo.name}}</span>
    </div>
  {{/each}}
</ul>
